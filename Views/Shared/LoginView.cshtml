@if (Request.IsAuthenticated)
{
    <text>Welcome <b>@User.Identity.Name</b> ! 
    [@Html.ActionLink("Log Off", "LogOff", "Account")]  
    [@Html.ActionLink("Change Password", "ChangePassword", "Account")]</text>
    
}
else
{
    @:[@Html.ActionLink("Log On", "LogOn", "Account")]
}